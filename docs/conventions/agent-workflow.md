<!-- generated:start cap:agent-workflow -->
# Agent Workflow — Aether Agent Workspace

Seeded scaffold; the workflow below is agent/team-owned.
<!-- generated:end cap:agent-workflow -->

## Per-change workflow

1. Read the operating contract and the relevant `docs/architecture/` pages.
2. Make the change; keep code and declared architecture coherent.
3. Update agent-owned doc sections you invalidated (never edit inside generated markers).
4. Consult the doc-change matrix when it exists (tooling-owned; may be absent).
